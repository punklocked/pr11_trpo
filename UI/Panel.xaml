<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="Window" BasedOn="{StaticResource MaterialDesignWindow}">
        <Setter Property="Height" Value="700"/>
        <Setter Property="Width" Value="500"/>
        <Setter Property="Background" Value="{DynamicResource WindowBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground}"/>
    </Style>

    <Style TargetType="DockPanel">
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>

    <Style TargetType="DockPanel" x:Key="MainPanel">
        <Setter Property="Background" Value="{DynamicResource Background}"/>
    </Style>

    <Style TargetType="Frame">
        <Setter Property="NavigationUIVisibility" Value="Hidden"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Background" Value="{DynamicResource Background}"/>
    </Style>

    <Style TargetType="StackPanel" x:Key="InfoPanel">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Background" Value="Gray"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Background" Value="{DynamicResource Background}"/>
    </Style>

    <Style TargetType="StackPanel" x:Key="InfoPanelItem">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Background" Value="{DynamicResource Background}"/>
    </Style>

    <Style TargetType="StackPanel" x:Key="HorizontalPanel">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Background" Value="{DynamicResource Background}"/>
    </Style>

    <Style TargetType="StackPanel" x:Key="VerticalPanel">
        <Setter Property="MinWidth" Value="300"/>
        <Setter Property="Orientation" Value="Vertical"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Background" Value="{DynamicResource Background}"/>
    </Style>

</ResourceDictionary>